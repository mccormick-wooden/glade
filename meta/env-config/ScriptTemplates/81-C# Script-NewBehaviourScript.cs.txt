using System.Collections;
using System.Collections.Generic;
using UnityEngine;

    #ROOTNAMESPACEBEGIN#
public class #SCRIPTNAME# : MonoBehaviour
{
    // MonoBehaviour method documentation - https://docs.unity3d.com/ScriptReference/MonoBehaviour.html
    
    // Awake is called when the script is loaded
    private void Awake()
    {
    }

    // Start is called before the first frame update
    private void Start()
    {
    }

    // OnEnable is called when the object becomes enabled and active.
    private void OnEnable()
    {
    }

    // OnDisable is called when the behaviour becomes disabled, or when the object is destroyed.
    private void OnDisable()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }

    // FixedUpdate at a fixed time interval, regardless of framerate (default, every 0.02 seconds)
    private void FixedUpdate()
    {
    }

    // LateUpdate is called after all Update functions have been called (e.g. cameras should update here)
    private void LateUpdate()
    {
    }
}
#ROOTNAMESPACEEND#
